import psycopg2

conn = psycopg2.connect(user="agv",
                        password="123456hadz",
                        host="localhost",
                        port="5432",
                        database="agv_database")